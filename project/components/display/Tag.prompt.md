# Tag

A chip for categories, filters, or selectable options. Optional icon and a remove (×) button.

```jsx
<Tag icon="droplet">PCOS</Tag>
<Tag selected>Fertility</Tag>
<Tag onRemove={() => …}>High-risk pregnancy</Tag>
```

Props: `icon`, `selected`, `onRemove`.

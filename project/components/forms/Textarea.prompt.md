# Textarea

Labelled multi-line text field, matching Input's warm frame and focus ring. Vertically resizable.

```jsx
<Textarea label="Your message" rows={5} placeholder="Tell us how we can help…" />
<Textarea label="Notes" error="This field is required" />
```

Props: `label`, `hint`, `error`, `rows`, plus native textarea attrs.

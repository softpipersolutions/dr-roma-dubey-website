# Canonical production origin — used for SEO (canonical URLs, sitemap,
# Open Graph, structured data). Set this to your real domain in the
# hosting environment (e.g. Vercel → Project → Settings → Environment
# Variables). No trailing slash.
NEXT_PUBLIC_SITE_URL=https://drromadubey.com
